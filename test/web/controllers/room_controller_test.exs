defmodule Web.RoomControllerTest do
  use Web.ConnCase
  use JanusEx.RoomCase

  # TODO eh ...
  # describe "GET /" do
  #   test "when there are no rooms", %{conn: conn} do
  #     conn = get(conn, "/")
  #     assert conn.status == 200

  #     html = Floki.parse(conn.resp_body)

  #     assert [] == Floki.find(html, "section#rooms > ul.list.pl0")
  #   end

  #   test "when there are rooms"
  # end
end
